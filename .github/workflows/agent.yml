name: Affiliate Agent

on:
  workflow_dispatch:
  schedule:
    - cron: "0 12 * * *"

jobs:
  run-agent:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run agent
        run: python agent.py
